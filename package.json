{
  "name": "reverse-code-executor",
  "version": "1.0.0",
  "description": "A program to execute code in reverse, prototype.",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fs": "^0.0.1-security"
  }
}
