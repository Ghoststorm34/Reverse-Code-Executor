# Reverse Code Executor
Prototype for a reverse code executor.
Uses txt files with JavaScript code written in Reverse, and then executes that code.

Built in Visual Studio Code, with npm for local execution.

## Libraries Used
- fs
